<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <%= render 'my_events' %>
      <%= render 'upcoming_events' %>
      <%= render 'past_events' %>
    </div>
  </div>
</section>

